- url_options = Hash[:action => 'index', :neighborhood => nil, :zip => nil]
- url_options.update(:tag => tag.parameterize) unless tag.blank?
- url_options.update(:query => query.parameterize) unless query.blank?

%h4.city.nearby.bottom== Nearby Cities
%ul
  - nearby_cities.each do |city|
    %li= link_to(city.name, url_for(url_options.update(:city => city)), :class => 'city nearby')