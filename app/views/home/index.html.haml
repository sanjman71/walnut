- title('Looking for a Place?')

.span-20.prepend-1.padding-top-10
  %h2
    == Its not always easy to find a local business online.  We are trying to fix that by making it really easy to find the places you are looking for.
    
    %hr.space
    
    %span{:style => 'font-size: 0.7em;'}== Browse Popular Cities:
    %br
    - @cities.each do |city|
      - klasses = ["locality"].push(cycle('even', 'odd'))
      %span.small{:style => "white-space: nowrap;"}= link_to city.name, url_for(:controller => 'places', :action => 'city', :country => @country, :state => city.state, :city => city), :class => klasses.join(" ")

    %hr.space
    
    %span{:style => 'font-size: 0.7em;'}= link_to 'Browse by state', url_for(:controller => 'places', :action => 'country', :country => @country)
    
        