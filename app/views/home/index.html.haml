- title('Looking for a Place?')

.span-20.prepend-1
  %h2
    == Its not always easy to find a local business online.  We are trying to fix that by making it really easy to find the places you are looking for.
    
    %p
    == Browse Popular Cities:
    %br
    - @cities.each do |city|
      = link_to city.name, url_for(:controller => 'places', :action => 'city', :country => @country, :state => city.state, :city => city)

    %p
    = link_to 'Browse by state', url_for(:controller => 'places', :action => 'country', :country => @country)
        