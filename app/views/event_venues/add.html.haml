- title(@title)

.span-23.mat.prepend-1.last.padding-top-10.padding-bottom-10
  %h2= @h1
  %h4#breadcrumbs
    = link_to 'Back', request.referer
    
%hr.space

= render :partial => 'locations/edit_location', :object => @location, :locals => {:form_submit => 'Add location', :submit_url => locations_path, :event_venue_id => @venue.id}
